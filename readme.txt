"""addr_api.py is main program to be run"""
python addr_api.py (is the commamnd to run)

program result can be seen at local host  http://127.0.0.1:5000/addressess/any ip address from csv/any subnet mask from csv
or http://127.0.0.1:5000/reports/any ip address from csv/any subnet mask from csv

addr.csv is the csv file where we do have required data as ID,OBJECTNAME,IPADDRESS,SUBNETMASK,OWNER and all

RequiredData.py is having functions which we are importing in addr_api.py

templates folder is having reports.html to convert data into html table